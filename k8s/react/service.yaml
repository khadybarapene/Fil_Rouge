apiVersion: v1
kind: Service
metadata:
  name: react-service
  namespace: portfolio
spec:
  selector:
    app: portfolio-react
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
  type: NodePort
